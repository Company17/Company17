DATABASE_URL=            # Neon pooled connection (used through Hyperdrive in production)
DIRECT_URL=              # Neon direct connection (migrations only)
AUTH_SECRET=
AUTH_RESEND_KEY=
AUTH_EMAIL_FROM=
NEXT_PUBLIC_APP_URL=
# R2 access is via the SHEETS binding in wrangler.jsonc, not env vars
